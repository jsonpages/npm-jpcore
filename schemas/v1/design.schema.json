
### Beat 1 — Il comando (0:00 → 0:06, 6s)

**Cosa si vede:** hard cut dal segmento 3. Terminal pulito, nero, font monospace grande. Tre comandi si digitano (o appaiono, accelerati) in sequenza:

```
$ npx @olonjs/cli new tenant demo
$ cd demo
$ npm install && npm run dev
```

Il cursore lampeggia. Si vede chiaramente **`@olonjs/cli`** nel primo comando — il nome del package, che è il fulcro.

**Voice:** *"Everything you've seen starts here. Three commands."*

**Ruolo:** ancoraggio visivo. Il reviewer fino a questo punto ha visto dashboard, MCP calls, Gmail, JSON files, spec. Ora torna al terminal nudo — è il "ecco il ground truth". 6 secondi: abbastanza per leggere i tre comandi, non così tanto da diventare noia. Il cambio di contesto (tutto su nero) è intenzionale — stacca nettamente dal segmento 3.

---

### Beat 2 — Il tenant scaffoldato (0:06 → 0:11, 5s)

**Cosa si vede:** il comando gira (accelerato 5-8x in editing), il CLI output scorre brevemente con le righe dei file creati. Poi stacco su file tree in editor (Cursor, VS Code, qualunque) che mostra la struttura scaffoldata:

```
demo/
├── src/
│   ├── App.tsx
│   ├── data/
│   │   ├── config/
│   │   │   ├── site.json
│   │   │   ├── theme.json
│   │   │   └── menu.json
│   │   └── pages/
│   │       └── home.json
│   └── index.css
├── package.json
└── tsconfig.json
```

Poi flash 1-2 secondi sul browser che apre `localhost:5174` — tenant vuoto renderizzato, stilizzato minimale ma presente.

**Voice:** *"A tenant that speaks OlonJS v1.6 from scratch. Empty, but conformant."*

**Ruolo:** far vedere **cosa produce** il CLI. Non un hello-world, ma **un tenant strutturato con i file del contratto** (`theme.json`, `menu.json`, `site.json`, `pages/*.json`). Il reviewer tecnico nota che ci sono già i file del contratto OlonJS — non è un boilerplate React generico. *"Empty but conformant"* è la frase tecnica precisa: è vuoto di contenuti, ma pienamente conforme al protocollo v1.6.

---

### Beat 3 — Il reveal B2B (0:11 → 0:17, 6s)

**Cosa si vede:** stacco su un frame simbolico — potrebbero essere tre logo affiancati (o tre testi mono-spaced) di builder AI riconoscibili, senza marchiarli ufficialmente: `Bolt`, `Replit`, `Lovable`, `v0`. Oppure, più asciutto: una riga di codice che mostra il CLI integrato programmaticamente, tipo `execSync('npx @olonjs/cli new tenant ' + userBrand)` all'interno di un loop di pipeline. Scegliere quello che rende più chiaro il pattern integrazione.

Alternativa meno diretta e più elegante: un diagramma minimalista di tre box — `AI site builder` → `@olonjs/cli` → `Conformant tenant` — con una freccia pulita.

**Voice:** *"The same primitive our agent uses is the one any AI site builder can plug in. One npm package. MIT license. The contract is the product."*

**Ruolo:** **il reveal strategico del video intero.** Qui chiudi il posizionamento B2B che hai tenuto sottilmente per tutto il video. *"The same primitive our agent uses"* fa vedere che olon-agent (segmento 3) non è speciale — usa lo stesso strumento pubblico. *"One npm package. MIT license."* sono i due fatti che chiudono qualunque obiezione su lock-in o visibilità tecnica. *"The contract is the product"* è il claim strategico definitivo: il prodotto vero non è il tool di scaffolding, è la spec che rende il tool possibile.

6 secondi perché c'è molto da dire, ma le frasi sono corte e devono "cadere" una alla volta con micro-pause — non lette veloci.

---

### Beat 4 — Respiro finale (0:17 → 0:20, 3s)

**Cosa si vede:** il frame del Beat 3 resta fermo 2 secondi, poi stacco graduale (hard cut, non fade) verso il frame iniziale dell'outro.

**Voice:** silenzio. Musica lo-fi continua.

**Ruolo:** **il silenzio vende più delle parole qui.** Dopo *"the contract is the product"*, qualunque parola aggiuntiva indebolisce il claim. 3 secondi di silenzio mentre il messaggio "atterra" nella testa del reviewer. Questa è la punteggiatura finale.

---

## Riepilogo tabellare — Segmento 4

| Beat | Tempo | Cosa vede | Voice (Claire) |
|---|---|---|---|
| 1 | 0:00–0:06 | Terminal con `npx @olonjs/cli new tenant demo` + due comandi successivi | *"Everything you've seen starts here. Three commands."* |
| 2 | 0:06–0:11 | File tree scaffoldato + flash tenant vuoto renderizzato | *"A tenant that speaks OlonJS v1.6 from scratch. Empty, but conformant."* |
| 3 | 0:11–0:17 | Logo/testi builder AI, o diagramma integrazione | *"The same primitive our agent uses is the one any AI site builder can plug in. One npm package. MIT license. The contract is the product."* |
| 4 | 0:17–0:20 | Frame fermo, silenzio | — |

---

# Outro · 10 secondi

**Messaggio strategico:** chiusura identitaria. Nessun claim nuovo, nessun pitch. Solo firma e call-to-action sottile.

---

### Beat 1 — Brand mark (0:00 → 0:04, 4s)

**Cosa si vede:** schermo quasi nero o neutro. Logo OlonJS al centro (testuale, tipografico — il wordmark). Sotto, una riga sola: `olon.js.org`. Niente altro. La musica lo-fi sale leggermente (+3dB) per marcare la chiusura.

**Voice:** silenzio. Solo musica.

**Ruolo:** dare identità al video. Il reviewer esce con il nome nella testa, non con un claim. I founder video di maggior successo YC finiscono quasi sempre in silenzio con logo + link — è la firma.

---

### Beat 2 — Riga di contesto (0:04 → 0:08, 4s)

**Cosa si vede:** sotto il logo appare una seconda riga, in font più piccolo e colore attenuato:

```
OlonJS — the contract layer for the agentic web
```

### Beat 3 — Respiro di chiusura (0:08 → 0:10, 2s)

**Cosa si vede:** schermo tenuto fermo per 2 secondi. La musica lo-fi sfuma gradualmente.

**Voice:** silenzio.

**Ruolo:** non tagliare bruscamente. Un video che finisce di colpo sembra mal montato. 2 secondi di silenzio + fade musicale danno chiusura professionale.

---

## Riepilogo tabellare — Outro

| Beat | Tempo | Cosa vede | Voice |
|---|---|---|---|
| 1 | 0:00–0:04 | Logo OlonJS + `olon.js.org` | Silenzio, musica |
| 2 | 0:04–0:08 | `OlonJS — the contract layer for the agentic web` | Silenzio |
| 3 | 0:08–0:10 | Frame fermo, fade musica | Silenzio |

---

---

